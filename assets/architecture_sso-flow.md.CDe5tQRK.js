import{M as e}from"./chunks/theme.DgT5h_PA.js";import{I as r,c as s,o as l,aF as o,J as c}from"./chunks/framework.CZ2lsOHd.js";const m=JSON.parse('{"title":"SSO Authentication Flow","description":"OAuth2 and OpenID Connect (OIDC) implementation details","frontmatter":{"title":"SSO Authentication Flow","description":"OAuth2 and OpenID Connect (OIDC) implementation details"},"headers":[],"relativePath":"architecture/sso-flow.md","filePath":"architecture/sso-flow.md"}'),d={name:"architecture/sso-flow.md"},b=Object.assign(d,{setup(h){const a=[{id:"client",type:"multi-handle",label:"Client / Frontend",position:{x:0,y:150}},{id:"api",type:"multi-handle",label:"API (SSO Module)",position:{x:300,y:150}},{id:"provider",type:"multi-handle",label:"Provider (Google/GitHub)",position:{x:650,y:50}},{id:"db",type:"multi-handle",label:"Database",position:{x:650,y:250}}],i=[{id:"e1",source:"client",target:"api",sourceHandle:"right-source",targetHandle:"left",label:"1. Login Request",type:"smoothstep",animated:!0,markerEnd:e.ArrowClosed},{id:"e2",source:"api",target:"provider",sourceHandle:"top-source",targetHandle:"left",label:"2. Redirect /authorize",type:"smoothstep",animated:!0,markerEnd:e.ArrowClosed},{id:"e3",source:"provider",target:"api",sourceHandle:"bottom-source",targetHandle:"top",label:"3. Callback with ?code",type:"smoothstep",animated:!0,markerEnd:e.ArrowClosed},{id:"e4",source:"api",target:"provider",sourceHandle:"right-source",targetHandle:"bottom",label:"4. Token Exchange & JWT Verify",type:"smoothstep",animated:!0,markerEnd:e.ArrowClosed},{id:"e5",source:"api",target:"db",sourceHandle:"bottom-source",targetHandle:"left",label:"5. Find/Create Identity",type:"smoothstep",animated:!0,markerEnd:e.ArrowClosed},{id:"e6",source:"api",target:"client",sourceHandle:"left-source",targetHandle:"right",label:"6. Returns App Session",type:"smoothstep",animated:!0,markerEnd:e.ArrowClosed}];return(p,t)=>{const n=r("InteractiveFlow");return l(),s("div",null,[t[0]||(t[0]=o('<h1 id="sso-authentication-flow" tabindex="-1">SSO Authentication Flow <a class="header-anchor" href="#sso-authentication-flow" aria-label="Permalink to “SSO Authentication Flow”">​</a></h1><p>This guide covers the Single Sign-On (SSO) integration using OAuth 2.0 and OpenID Connect (OIDC). The boilerplate supports robust integration with identity providers like <strong>Google</strong> and <strong>GitHub</strong>.</p><h2 id="architecture-overview" tabindex="-1">Architecture Overview <a class="header-anchor" href="#architecture-overview" aria-label="Permalink to “Architecture Overview”">​</a></h2><p>The SSO flow is completely built into the core domain layer (<code>src/domain/sso</code>), avoiding heavy framework coupling and using the clean distinction between <em>Identities</em> and <em>Credentials</em>.</p>',4)),c(n,{nodes:a,edges:i}),t[1]||(t[1]=o(`<h2 id="key-components" tabindex="-1">Key Components <a class="header-anchor" href="#key-components" aria-label="Permalink to “Key Components”">​</a></h2><h3 id="_1-get-authorize-ts-the-redirect" tabindex="-1">1. <code>get-authorize.ts</code> (The Redirect) <a class="header-anchor" href="#_1-get-authorize-ts-the-redirect" aria-label="Permalink to “1. get-authorize.ts (The Redirect)”">​</a></h3><p>The entrypoint <code>/authorize</code> generates the necessary URL to redirect the user to the provider.</p><ul><li>It includes security measures such as <code>state</code> generation to prevent CSRF attacks.</li><li>Certain providers, such as Google, might also require a <code>nonce</code> depending on the requested <code>response_type</code>.</li></ul><h3 id="_2-get-callback-ts-the-token-exchange-login" tabindex="-1">2. <code>get-callback.ts</code> (The Token Exchange &amp; Login) <a class="header-anchor" href="#_2-get-callback-ts-the-token-exchange-login" aria-label="Permalink to “2. get-callback.ts (The Token Exchange &amp; Login)”">​</a></h3><p>Once the IDP redirects the user back to the application (<code>/callback</code>), this controller:</p><ul><li>Extracts the authorization <code>code</code> from the query string.</li><li>Validates the <code>state</code> to ensure the session wasn&#39;t hijacked.</li><li>Calls the underlying <code>exchangeToken</code> infrastructure to swap the <code>code</code> for an <code>access_token</code> (and <code>id_token</code> if applicable).</li><li>Generates or updates the internal <code>Identity</code> and <code>Credential</code>.</li><li>Finally, it provides the internal <code>JWT</code> used by the application for subsequent requests.</li></ul><h3 id="_3-oidc-ts-the-infrastructure-hook" tabindex="-1">3. <code>oidc.ts</code> (The Infrastructure Hook) <a class="header-anchor" href="#_3-oidc-ts-the-infrastructure-hook" aria-label="Permalink to “3. oidc.ts (The Infrastructure Hook)”">​</a></h3><p>Located at <code>src/infrastructure/sso/oidc.ts</code>, this file manages all the external communication with the providers:</p><ul><li><code>exchangeToken</code>: Hits the provider&#39;s token endpoint to trade codes.</li><li><code>verifyIdToken</code>: A robust built-in JWT validator that fetches the provider&#39;s Public Keys (JWKS), verifies the <code>kid</code> (Key ID), and ensures the <code>exp</code> (Expiration) and signature are completely valid before trusting the token.</li><li><code>getNormalizedUser</code>: Maps different API contracts (e.g. Google&#39;s parsed <code>id_token</code> vs GitHub&#39;s <code>/user</code> endpoint) into a consistent format.</li></ul><h2 id="environment-configuration" tabindex="-1">Environment Configuration <a class="header-anchor" href="#environment-configuration" aria-label="Permalink to “Environment Configuration”">​</a></h2><p>To enable SSO, setup the specific environment variables in your <code>.env</code>:</p><div class="language-env"><button title="Copy Code" class="copy"></button><span class="lang">env</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span># Google SSO</span></span>
<span class="line"><span>SSO_GOOGLE_CLIENT_ID=&quot;your-client-id.apps.googleusercontent.com&quot;</span></span>
<span class="line"><span>SSO_GOOGLE_CLIENT_SECRET=&quot;your-client-secret&quot;</span></span>
<span class="line"><span>SSO_GOOGLE_REDIRECT_URI=&quot;http://localhost:3000/api/v1/sso/callback&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># GitHub SSO</span></span>
<span class="line"><span>SSO_GITHUB_CLIENT_ID=&quot;your-client-id&quot;</span></span>
<span class="line"><span>SSO_GITHUB_CLIENT_SECRET=&quot;your-client-secret&quot;</span></span>
<span class="line"><span>SSO_GITHUB_REDIRECT_URI=&quot;http://localhost:3000/api/v1/sso/callback&quot;</span></span></code></pre></div><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>If a provider is not configured, the boilerplate will instantly throw an error if a user attempts to use <code>/authorize</code> for that provider.</p></div><h2 id="security-considerations" tabindex="-1">Security Considerations <a class="header-anchor" href="#security-considerations" aria-label="Permalink to “Security Considerations”">​</a></h2><ol><li>The OAuth flow uses <strong>JWKS validation arrays</strong> to ensure <code>id_tokens</code> received from providers like Google belong to them and weren&#39;t tampered with.</li><li>We enforce <code>state</code> comparison across both GitHub and Google to guarantee CSRF resistance.</li><li>Access tokens inside the application are extremely short-lived, favoring refresh tokens.</li></ol>`,16))])}}});export{m as __pageData,b as default};

## 2024-03-02 - [Optimize SSO Local Login] | Learning: Sequential queries to fetch related entities during authentication (like identity and credentials) cause an N+1 query problem which negatively impacts latency. | Action: Combine sequential authentication queries using Drizzle's `innerJoin` to fetch all necessary data in a single database roundtrip, effectively reducing query time and latency.

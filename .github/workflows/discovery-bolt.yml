name: Agency Discovery (Bolt)

on:
  workflow_dispatch:

jobs:
  bolt:
    runs-on: ubuntu-latest

    concurrency:
      group: agency-bolt
      cancel-in-progress: false

    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout (staging)
        uses: actions/checkout@v4
        with:
          ref: staging

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Run Bolt Agent
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bun src/automations/discovery-bolt.ts